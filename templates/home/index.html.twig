{% extends 'base.html.twig' %}

{% block title %}Accueil{% endblock %}

{% block body %}
<div class="container mt-5">
    <div class="text-center mb-4">
        <h1 class="display-4">Bienvenue sur notre application</h1>
        <p class="lead">Cette application démontre la mise en place d'un système d'authentification complet avec Symfony.</p>
        <p>Vous pouvez vous inscrire, vous connecter, gérer votre profil et bien plus encore.</p>
    </div>

    <div class="text-center mb-4">
        {% if not app.user %}
            <a href="{{ path('app_register') }}" class="btn btn-primary me-2">S'inscrire</a>
            <a href="{{ path('app_login') }}" class="btn btn-secondary">Se connecter</a>
        {% else %}
            <a href="{{ path('app_profile') }}" class="btn btn-success me-2">Mon profil</a>
            {% if is_granted('ROLE_ADMIN') %}
                <a href="{{ path('app_admin') }}" class="btn btn-danger">Administration</a>
            {% endif %}
        {% endif %}
    </div>

    <div class="row">
        <div class="col-md-4">
            <div class="card shadow-sm mb-4">
                <div class="card-body">
                    <h5 class="card-title">🔐 Authentification sécurisée</h5>
                    <p class="card-text">Système d'authentification complet avec inscription, connexion et gestion des rôles.</p>
                </div>
            </div>
        </div>
        <div class="col-md-4">
            <div class="card shadow-sm mb-4">
                <div class="card-body">
                    <h5 class="card-title">📧 Vérification par email</h5>
                    <p class="card-text">Vérification des comptes par email et système de réinitialisation de mot de passe.</p>
                </div>
            </div>
        </div>
        <div class="col-md-4">
            <div class="card shadow-sm mb-4">
                <div class="card-body">
                    <h5 class="card-title">👥 Gestion des utilisateurs</h5>
                    <p class="card-text">Interface d'administration pour gérer les utilisateurs et leurs permissions.</p>
                </div>
            </div>
        </div>
    </div>
</div>
{% endblock %}
