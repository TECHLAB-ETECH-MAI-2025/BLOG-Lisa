{# templates/article/_actions.html.twig #}
<div class="btn-group " role="group">
    <a href="{{ path('app_article_show', {id: article.id}) }}" class="btn btn-sm btn-primary me-2 bi bi-eye">
        Voir
    </a>
    <a href="{{ path('app_article_edit', {id: article.id}) }}" class="btn btn-sm btn-warning me-2 bi bi-pencil">
    >
        Modifier
    </a>
    {{ include('article/_delete_form.html.twig') }}
    <input type="hidden" name="_token" value="{{ csrf_token('like' ~ article.id) }}">
    
</div>